namespace BounceHeroes.Core
{
    /// <summary>
    /// 한 판(런)의 플레이 점수를 등급으로 환산한 결과입니다. 결과 화면에 배지로 표시됩니다.
    /// 값 순서가 곧 등급 크기이므로 재배치하지 마세요.
    /// </summary>
    public enum ScoreGrade
    {
        D = 0,
        C = 1,
        B = 2,
        A = 3,
        S = 4,
    }
}
